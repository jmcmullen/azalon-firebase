{
  "name": "azalon-firebase",
  "version": "1.0.0",
  "description": "Product key & licencing system",
  "scripts": {
    "clean": "mkdir -p public && rm -rf public/*",
    "cp:static": "cp -R functions/nuxt/dist/ public && cp -R src/static/* public",
    "setup": "npm run clean && npm run cp:static",
    "serve": "npm run setup && sudo firebase serve --only hosting,functions",
    "deploy": "npm run setup && firebase deploy"
  },
  "keywords": [
    "nuxt.js",
    "firebase"
  ],
  "author": "Jay McMullen",
  "license": "ISC",
  "eslintIgnore": [
    "functions/*"
  ]
}
